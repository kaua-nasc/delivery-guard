name: Deploy API to Server

on:
  push:
    branches: 
      - "develop"
      - "master"

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      folder: api
    secrets: inherit