name: Deploy API to Server

on:
  push:
    paths: 
      - api/**
      - deploy.yml
      - deploy-api.yml
    branches: 
      - "develop"
      - "master"

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      folder: api
    secrets: inherit